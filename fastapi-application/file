[Unit]
Description=Animal Photo Generator app with Gunicorn
After=network.target

[Service]
User=khanh
Group=www-data
WorkingDirectory=/path/to/your/project
ExecStart=/home/khanh/.pyenv/versions/bugtrackerenv/bin/gunicorn app.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:4000
Restart=always

[Install]
WantedBy=multi-user.target
